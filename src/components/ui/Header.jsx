// src/components/Header.jsx

/**
 * 📌 Header
 * ------------------
 * Minimal placeholder header with fixed height (64px).
 *
 * Props:
 * - None
 *
 * Usage:
 * - Extend this component with Navbar, logo, or branding if needed.
 */
export default function Header() {
  return (
    <header style={{ height: 64 }}>
      {/* Empty placeholder header (64px height) */}
    </header>
  );
}
